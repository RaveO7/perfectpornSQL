/** @type {import('next').NextConfig} */

const nextConfig = {
    images: {
      remotePatterns: [
        {
          protocol: 'https',
          hostname: 'hdporn92.com',
          hostname: 'courir.com',
          hostname: 'fxpornhd.com'
        },
      ],
      domains: ['www.courir.com'],
    },
  }

module.exports = nextConfig